'use strict';

/**
 * Implement createStore function:
 *
 * Function takes 2 arguments and returns an object
 *
 * @param {function} reducer
 * @param {object} initialState
 *
 * @returns {object}
 */
const createStore = () => {
  // write code here
};

module.exports = createStore;
